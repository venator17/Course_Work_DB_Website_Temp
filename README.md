### 🚀 Інструкція для запуску (Чиста установка)

**Вимоги:**
Встановлений **Docker Desktop** (Windows/Mac) або **Docker Engine** (Linux).

#### Крок 1. Запуск

1.  Розпакуй архів.
2.  Відкрий термінал у папці з файлом `docker-compose.yml`.
3.  Виконай команду для збірки та запуску:
    ```bash
    docker-compose up --build
    ```
    *Дочекайся повідомлення в логах:* `Starting development server at http://0.0.0.0:8000/`

#### Крок 2. Ініціалізація бази даних (Обов'язково)

Оскільки база даних чиста, потрібно створити таблиці. Відкрий **нове** вікно терміналу (не закриваючи попереднє) і виконай:

1.  **Застосувати міграції (створення структури БД):**

    ```bash
    docker-compose exec web python manage.py migrate
    ```

2.  **Створити адміністратора (для входу в адмінку):**

    ```bash
    docker-compose exec web python manage.py createsuperuser
    ```

    *(Введи логін та пароль, коли система попросить).*

#### Крок 3. Доступ

  * **Головна сторінка:** [http://127.0.0.1:8000](https://www.google.com/search?q=http://127.0.0.1:8000)
  * **Адмін-панель:** [http://127.0.0.1:8000/admin](https://www.google.com/search?q=http://127.0.0.1:8000/admin)

#### Зупинка проєкту

Натисни `Ctrl+C` у вікні з логами або виконай команду:

```bash
docker-compose down
```